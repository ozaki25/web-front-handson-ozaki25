# CLAUDE.md

このリポジトリを Claude Code などの AI アシスタントと一緒に書き続けるためのガイドです。
レッスンを追加・改訂する前に必ず読み込んでください。

## 1. このプロジェクトは何か

- 名前: `web-front-handson-ozaki25`
- 一言で: **Web フロントエンドをこれから学ぶ人が、手を動かしながら基礎をゼロから積み上げられるチュートリアル。**
- 兄弟リポ: [`web-front-training-ozaki25`](https://github.com/ozaki25/web-front-training-ozaki25)（読み物中心・議論型）
  - 体裁（VitePress / ブランチ戦略 / 執筆方針）は基本踏襲する。
  - 中身は**ハンズオン主体**で差別化する。

## 2. 読者像

- コードはほぼ書いたことがない、または HTML をすこし触った程度。
- 「読むだけで分かる人」は**ターゲットではない**。
- 用語は初耳前提。初出で 1 行定義する。

## 3. 完走ゴール

- Next.js (App Router) で**小さなアプリ**を自力で作れる。
- 例: フォーム付きページ / データを表示するページ / ルーティングのあるページ。

## 4. カリキュラムの大枠

6 部構成。途中での再編はあり。

1. Web の素材 (HTML / CSS)
2. 動きをつける (JavaScript / DOM)
3. 型と道具 (TypeScript / Vite / npm)
4. React
5. Next.js (App Router)
6. 仕上げ — 小さなアプリを作る

- レッスン単位は `lessonXX`（`dayXX` は使わない）。
  - 毎日 1 レッスンに縛らない。**1 日で一気に複数レッスン進める**使い方も想定する。
- 全体ボリュームの上限は決めない。書きながら増やす。

## 5. 配信形態・実行環境

- **VitePress** で生成し、Vercel で公開。
- 学習者のコード実行環境は **StackBlitz を基本** とする。
  - 理由: アカウント不要・即起動・無料、かつ WebContainers で Next.js まで動く。
  - 各レッスンから **「StackBlitz で開く」リンク or 埋め込み** を貼って、読みながら即試せる状態にする。
- ローカル環境構築にページを割かない。独立した「準備章」は設けない。
- `.devcontainer/devcontainer.json` は**将来の Codespaces 併用のために残す**（現行コンテンツでは案内しない）。
- ローカル開発への移行は、最終レッスン or 付録で「実務への接続」としてだけ触れる。

## 6. 執筆 7 原則

1. **動く → なぜ** の順。動くものを先に見せ、後から仕組みを説明する。
2. **コードは省略しない**。import も閉じタグも全部書く。
3. **用語は初出で 1 行定義**。
4. **期待出力を必ず示す**。スクショ or 「ここで画面に 〜 と出るはず」。
5. **コピペ → 変える → 自分で書く** の 3 段階を各レッスンで回す。
6. **わざと壊す体験** を序盤で入れる（エラーを読んで直す）。
7. **安定版のみ**前提にする（Next.js / TypeScript / Tailwind の stable）。

## 7. レッスン構成テンプレ

各 `lessonXX.md` は以下の固定順で書く。

```
# lessonXX: タイトル

## このレッスンで作るもの
## やってみる（コピペで動かす）
## 変えてみる
## 自分で書いてみる
## なぜ動く？（仕組みの説明）
## ふりかえり
## 次のレッスン
```

- 「やってみる / 変えてみる / 自分で書いてみる」は**到達ゴールと期待出力を必ず書く**。
- 解答例は折りたたみ、または別ファイルに置く。
- 1 レッスンで新しく導入する概念は原則 1 つまで。

## 8. ディレクトリ構成

```
docs/
├── .vitepress/
├── index.md
├── introduction/
└── lessons/
    ├── lesson01/
    │   └── index.md
    └── ...
docs/drafts/         ← 検討中のレッスン（main にマージしない）
docs/rejected/       ← 却下したレッスン案
.devcontainer/       ← Codespaces 設定
```

## 9. ブランチ運用

- `main`: 公開済みのレッスン・プロジェクト方針のみ。
- `draft`: レッスンの下書き・検討中カリキュラム。
- `publish/lessonXX`: `draft` から `main` に出すときのレッスン単位の作業ブランチ。

新しくレッスンを書き起こすときは **必ず `draft` から** 始める。`main` に直接書き足さない。

## 10. 新規レッスンを追加するときの手順（AI 向け）

1. `docs/lessons/` と `docs/drafts/` を見て、内容の重複や順序の破綻がないか確認する。
2. 対象技術の **stable 版バージョン** を確認し、本文中のバージョン表記を合わせる。
3. 「レッスン構成テンプレ」に従って構造を作る。
4. コードは**そのまま動く完全なもの**だけを置く（抜粋にしない）。
5. 必要に応じて VitePress のタブ / Mermaid / 折りたたみを使う。
6. ダークモード色指定を忘れない（白背景だけ指定して文字が読めなくなる事故を防ぐ）。
7. まず `docs/drafts/` に置く → 学習者目線でレビュー → `publish/lessonXX` に切り出す。

## 11. AI に避けてほしいこと

- 長い前置き（「〜について詳しく説明します」等）。
- 読者の前提知識を当てにした省略（「ご存知の通り」等）。
- 古い API / 非推奨のやり方（旧 `pages/` ルーター、クラスコンポーネント等）。
- 1 レッスン内でのレベル跳ね上がり。
- `…(省略)…` を使ったコード片。

## 12. 参考リポとの差分

|  | 参考 `web-front-training-ozaki25` | 今回 `web-front-handson-ozaki25` |
| --- | --- | --- |
| 主軸 | 読み物 + 議論 | 読みながら手を動かす |
| 想定レベル | ジュニア以上 | コード未経験でも入れる |
| 順序 | 「なぜ」→「動く」 | 「動く」→「なぜ」 |
| 単位 | `dayXX` | `lessonXX` |
| 実行環境 | ローカル想定 | StackBlitz で即開始（アカウント不要）|
